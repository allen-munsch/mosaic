# Mosaic

Semantic search engine using SQLite shards.

## Run

```bash
mix deps.get
mix run --no-halt
```

Or with Docker:

```bash
docker-compose up
```

## Test

```bash
curl http://localhost:4040/health
```
